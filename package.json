{
  "name": "autodownloader",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "python -m unittest && vitest run",
    "install": "npx puppeteer browsers install firefox && npx puppeteer browsers install chrome"
  },
  "dependencies": {
    "@types/node": "^22.10.2",
    "fs-ext": "^2.0.0",
    "puppeteer": "^23.11.0",
    "vitest": "^2.1.8",
    "tsx": "^4.19.2"
  }
}
